# ── Ramadan Fajr Bot — Environment Variables ──
# Copy to .env and fill in your values.  Secrets go in CI settings, not git.

# SiliconFlow API (image generation)
SILICONFLOW_API_KEY=sk_xxx
# Optional: override API base URL if your account uses a different host
SILICONFLOW_BASE_URL=https://api.siliconflow.com

# SMTP credentials (Gmail: use App Password, NOT your account password)
EMAIL_USER=you@example.com
EMAIL_PASS=app-specific-or-smtp-password
FROM_EMAIL=you@example.com

# Optional SMTP override (e.g., SendGrid)
SMTP_SERVER=smtp.sendgrid.net
SMTP_PORT=587

# SendGrid SMTP (preferred if Gmail blocks SMTP)
# Set SENDGRID_API_KEY and leave EMAIL_* blank if using SendGrid
SENDGRID_API_KEY=SG_xxx
SENDGRID_SMTP_HOST=smtp.sendgrid.net
SENDGRID_SMTP_PORT=587
SENDGRID_SMTP_USER=apikey

# Email-to-SMS gateway recipients (comma-separated)
# Format: <phone>@<carrier-gateway>
# Examples: 1234567890@vtext.com (Verizon), 19876543210@tmomail.net (T-Mobile)
SMS_RECIPIENTS=1234567890@vtext.com,19876543210@tmomail.net

# Location for Fajr calculation
LAT=40.7128
LON=-74.0060
TZ=America/New_York

# Local sent-marker directory (defaults to /tmp)
MARKER_DIR=/tmp

# Test mode (skips external API + SMTP; uses placeholder image)
RAMADAN_TEST_MODE=0

# ── Optional: S3 sent-markers (recommended for CI to prevent duplicates) ──
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET=your-bucket-name
