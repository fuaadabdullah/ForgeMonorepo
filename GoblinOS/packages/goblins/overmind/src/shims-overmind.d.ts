// Intentionally empty package shim - removed permissive overmind type declarations
// to avoid conflicts with the real `src/types.ts`. Keep this file present so
// any editors or tooling expecting it won't fail while we continue triage.

export {}
